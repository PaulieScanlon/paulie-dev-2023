---
import Main from '../layouts/main.astro';
import { getCollection } from 'astro:content';
import CollectionsList from '../components/collections-list.astro';

const demos = await getCollection('demos');
---

<Main>
  <strong class='text-xs text-brand-primary'>Demos</strong>
  <h1 class='mb-0'>Marketing Campaigns, Projects and Demos</h1>
  <p>
    I usually write and create example sites and repositories, but occasionally
    I'll design, and build, full production sites to demonstrate what's possible
    when you pop your thinking cap on.
  </p>
  <CollectionsList collections={demos} base='/demos' />
</Main>
